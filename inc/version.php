<?php
if ( !defined( 'VERSION' ) )
    define( 'VERSION', '0.2.2' );

if ( !defined( 'DB_VERSION' ) )
    define( 'DB_VERSION', 2 );

if ( !defined( 'HIGHCHARTS_VERSION' ) )
    define( 'HIGHCHARTS_VERSION', '3.0.4' );

if ( !defined( 'MIN_PHP_VERSION' ) )
    define( 'MIN_PHP_VERSION', '5.3.7' );

if ( !defined( 'MIN_MYSQL_VERSION' ) )
    define( 'MIN_MYSQL_VERSION', '5.5' );

